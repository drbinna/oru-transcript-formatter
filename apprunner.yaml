version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: gunicorn web_app:app --bind 0.0.0.0:8080 --timeout 600 --workers 2
  network:
    port: 8080
  env:
    - name: FLASK_ENV
      value: "production"
